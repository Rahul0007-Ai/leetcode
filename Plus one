class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        l=[]
        a=""
        for i in digits:
            a+=str(i)
        a=str(int(a)+1)
        for i in a:
            l.append(int(i))
        return l
